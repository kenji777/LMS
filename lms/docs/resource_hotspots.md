## ActivityModel_0002 리소스 집중 구간

| 구간 | 설명 | 영향 |
| --- | --- | --- |
| 블루투스 메시지 처리 (`btHandler`) | 수신 메시지마다 스레드 생성, DB 조회, 다중 `runOnUiThread` | CPU, 메모리, 메인 스레드 |
| 블루투스 재연결 루프 | 1초 주기로 모든 페어링 장치에 소켓 연결 시도 | Bluetooth 스택, CPU |
| USB 폴링 타이머 | 500ms 고정 폴링, 중복 타이머 생성 가능 | CPU, I/O, 배터리 |
| 테스트 스펙 JSON/DB 처리 | 전체 데이터 삭제 후 재삽입 + 전량 UI 반영 | I/O, 메모리 |
| 키오스크 모드 감시 | 500ms마다 `hideSystemUI` 호출 | 메인 스레드 |

> 최신 상태: 2025-12-02

